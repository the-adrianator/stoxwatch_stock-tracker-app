[build]
  command = "npm run build"
  publish = ".next"

# Remove the Netlify Next.js plugin as it's causing conflicts with Next.js 14 App Router
# [[plugins]]
#   package = "@netlify/plugin-nextjs"

